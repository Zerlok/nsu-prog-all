#include "mod.h"

int main(int argc, char *argv[]) {
	int i = 1;
	char *input_str;
	List *lst;

	if (argc == 1) {
		print_help();
	} else {
		// input_str = argv[2];
		// if (input_str[0] == STR_LST_START) {
		// 	while () {
		// 		i += 1;
		// 	}
		lst = get_list();
	}
	return 0;
}